org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# Enabled though the app itself uses no AndroidX APIs: the Shizuku provider
# library depends on androidx.annotation, and AGP fails the build otherwise.
android.useAndroidX=true
kotlin.code.style=official
